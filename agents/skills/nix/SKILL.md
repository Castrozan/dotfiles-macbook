---
name: nix
description: "Nix language and ecosystem expert. Use for: writing/debugging Nix expressions, lazy evaluation and fixed-points, derivations and overlays, module system internals (mkIf, mkMerge, types), flake design, ecosystem tools (devenv, direnv, cachix, agenix), rebuild workflow, and dotfiles repo conventions."
---

Umbrella for all Nix: language expertise, rebuild workflow, devenv shells, and dotfiles repo conventions.

For Nix language expertise (expressions, evaluation, module system, overlays), read `expert.md`.

For applying configuration changes (rebuild workflow, troubleshooting), read `rebuild.md`.

For development environments (devenv shells, packages, services), read `devenv.md`.

For this dotfiles repository (architecture, patterns, anti-patterns, directory organization), read `repo.md`.
