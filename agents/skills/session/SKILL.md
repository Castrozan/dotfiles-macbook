---
name: session
description: Session lifecycle and context preservation — deep work context management, git worktrees for parallel branches, tmux control for background processes, session restart and exit.
---

Unified skill covering session management, context preservation, and process control.

For deep work context management, read `deep-work.md`.

For git worktrees and parallel branches, read `worktrees.md`.

For tmux session and process control, read `tmux.md`.

For session restart, read `restart.md`.

For session exit, read `exit.md`.
