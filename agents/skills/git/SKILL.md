---
name: git
description: Git workflows including commit creation and history search. Use when user asks to commit, stage changes, prepare commit messages, search git history, or investigate past changes. Never git add -A.
---

Umbrella for git workflows. Commit capability (conventional format, diff analysis, specific-file staging) and history search.

For creating commits (staging, conventional format, diff analysis), read `commit.md`.
