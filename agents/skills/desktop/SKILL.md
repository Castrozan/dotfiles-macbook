---
name: desktop
description: Interact with the desktop: type text, send key combos, click/move/scroll/drag mouse, capture screenshots, read/write clipboard, media playback control, YouTube operations. Use for any non-browser GUI automation.
---

Desktop interaction and media control skill. Uses macOS-native tools: osascript for keyboard, mouse, and media events, screencapture for screenshots, pbcopy/pbpaste for clipboard.

For keyboard input (type text, send key combos), read `keyboard.md`.

For mouse control (click, move, scroll, drag), read `mouse.md`.

For screenshots (full desktop, active window, region), read `screenshot.md`.

For media playback (play, pause, volume, next, previous), read `media-control.md`.

For YouTube operations (video search, playlists), read `youtube.md`.
