name: evals
on:
  push:
    branches: [main]
    paths:
      - "agents/**"
      - "CLAUDE.md"

jobs:
  baseline-check:
    runs-on: ubuntu-latest
    if: ${{ vars.ENABLE_EVALS == 'true' }}
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install pyyaml
        run: pip install pyyaml

      - name: Check baseline for regression
        run: python3 agents/evals/run-evals.py --check-baseline

  skill-evals:
    runs-on: ubuntu-latest
    if: ${{ vars.ENABLE_EVALS == 'true' }}
    continue-on-error: true
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install pyyaml
        run: pip install pyyaml

      - uses: actions/setup-node@v5
        with:
          node-version: "20"

      - name: Install Claude CLI
        run: npm install -g @anthropic-ai/claude-code

      - name: Run evals
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
        run: python3 agents/evals/run-evals.py
